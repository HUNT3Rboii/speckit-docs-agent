# /speckit.ext.setup

Configure the backend connection for the Documentation Agent extension.

1. Prompt for the API base URL.
2. Prompt for the shared backend API key.
3. Write the values to the extension configuration file.
4. Register or confirm the current project via the backend project endpoint.
