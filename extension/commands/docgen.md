# /speckit.ext.docgen

Use the developer's active agent session to read the target markdown artifact and produce structured JSON matching the documented schema.

Then POST the structured payload to /api/artifacts/ingest-structured.
