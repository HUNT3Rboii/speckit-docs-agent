# /speckit.ext.regenerate <path>

Force a re-render for a specific artifact path, bypassing the normal dedupe check.

Use this when the render template or taxonomy changes but the markdown content has not.
