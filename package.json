{
  "name": "speckit-auto-ai-standalone",
  "displayName": "Speckit Auto-AI (Standalone Preview)",
  "description": "Markdown to typeset PDF documentation, with no Docker and no setup",
  "version": "0.1.0",
  "publisher": "HUNT3Rboii",
  "license": "MIT",
  "author": "Mohamed Yassine Reggui",
  "repository": {
    "type": "git",
    "url": "https://github.com/HUNT3Rboii/speckit-docs-agent"
  },
  "bugs": {
    "url": "https://github.com/HUNT3Rboii/speckit-docs-agent/issues"
  },
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Documentation",
    "Other"
  ],
  "main": "./out/src/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "speckitStandalone.convertCurrentFile",
        "title": "Speckit Preview: Convert Current File to PDF"
      },
      {
        "command": "speckitStandalone.openPanel",
        "title": "Speckit Preview: Open Panel"
      },
      {
        "command": "speckitStandalone.toggleConvertOnSave",
        "title": "Speckit Preview: Toggle Convert on Save"
      }
    ],
    "configuration": {
      "title": "Speckit Preview",
      "properties": {
        "speckitStandalone.convertOnSave": {
          "type": "boolean",
          "default": false,
          "description": "Convert a markdown file to PDF when it is saved."
        },
        "speckitStandalone.enrich": {
          "type": "boolean",
          "default": true,
          "description": "Use the editor's language model to add a summary, glossary and diagrams. Every claim is checked against your document before it is printed; unsupported ones are dropped."
        },
        "speckitStandalone.debounceMs": {
          "type": "number",
          "default": 1500,
          "minimum": 200,
          "description": "How long to wait after a save before converting."
        }
      }
    }
  },
  "scripts": {
    "fetch-runtimes": "node scripts/fetch-runtimes.mjs",
    "vendor-deps": "node scripts/vendor-deps.mjs",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "build": "npm run compile && npm run build:webview",
    "test": "npm run compile && node --test \"out/src/test/**/*.test.js\"",
    "test:python": "python -m pytest server/tests -q",
    "smoke": "npm run compile && node scripts/smoke-host.mjs",
    "package": "npm run build && vsce package --target win32-x64",
    "build:webview": "npm --prefix webview-ui run build"
  },
  "devDependencies": {
    "@types/node": "^20.19.9",
    "@types/vscode": "^1.90.0",
    "typescript": "^5.9.3"
  }
}
